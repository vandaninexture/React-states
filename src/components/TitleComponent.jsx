/* create functional component (TitleCOmponent) */
const TitleComponent = (props) => {
    return (
        /* display title using props */
        <h1>{props.title}</h1>
    );
}

export default TitleComponent;